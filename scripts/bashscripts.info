Sammlung von bash befehlen mehr oder weniger kommentiert

#bash bulk file renaming (bizmate.biz Bulk reame files in linux)
 for file in *; do mv $file "$file.txt" ;done
	 for file in *; do mv $file ${file%.txt} ;done

#Zufällige auswahl an dateien  verschieben (im ordner wo die zu verschiebenen dateien sind ausführen) geht auch mit cp
	shuf -zen3000 * | xargs -0 mv -t ../../train/labels

#Das kopiert aus dem label verzeichnis heraus alle bilder aus den image ordner in den richtigen image folder image folder muss bereits existieren
	for file in *; do cp ../../images/${file/txt/jpg} ../images/  ;done

#für die txt files die in die config kommen einfach das hier. XXX ist eins von test,train,valid
	for file in /home/pacheco/data/Master/bilddaten/cyclists/XXX/images/*; do echo $file > XXX.txt; done




#Es folgen die Inhalte mehr oder weniger der bashcripts den selfmade Datensatz aufzuteilen und die beiden dateien mit den bild pfaden zu erstellen
# cyclist löschen und labeldateien zufällig aufteilen
mkdir cyclists
rm cyclists/* -r
mkdir cyclists/train
mkdir cyclists/train/images
mkdir cyclists/train/labels
mkdir cyclists/test/images
mkdir cyclists/test/labels
cp selfmade/* cyclists/ -r
cd cyclists/labels
shuf -zen10000 * | xargs -0 mv -t ../test/labels
mv * ../train/labels

# aus den Label Ordnern heraus die images in die entsprechenden ordner kopieren
cd ../train/labels
for file in *; do mv ../../images/${file/txt/jpg} ../images/ ; done
cd ../images
#die dateien mit den Pfaden der Bilddateien erstellen
find `pwd` -name \*.jpg | sort -R > ../../train.txt
cd ../../test/labels
for file in *; do mv ../../images/${file/txt/jpg} ../images/ ; done
cd ../images
find `pwd` -name \*.jpg | sort -R > ../../test.txt

#Alternative für die Dateien mit den Pfaden zu den Bildern
find /home/pacheco/data/Master/bilddaten/cyclists/test/images -name *.jpg | sort -R > /home/pacheco/data/Master/bilddaten/cyclists/test.txt
find /home/pacheco/data/Master/bilddaten/cyclists/train/images -name *.jpg | sort -R > /home/pacheco/data/Master/bilddaten/cyclists/train.txt

# Dateien mit pfaden von beiden Datensätzen
cat benchmark/valid.txt >> test.tmp
cat benchmark/test.txt >> test.tmp
cat benchmark/train.txt >> train.tmp
cat cyclists/test.txt >> test.tmp
cat cyclists/train.txt >> train.tmp
cat train.tmp | sort -R > train.list
cat test.tmp | sort -R > test.list

